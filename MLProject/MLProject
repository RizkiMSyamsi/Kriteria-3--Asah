name: sales-linear-regression-ci

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      input_path: {type: str, default: "Sales-Transaction-v.4a_preprocessing.csv"}
    command: "python modelling.py --input_path {input_path}"
