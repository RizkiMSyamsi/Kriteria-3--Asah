name: "Sales Transaction - Linear Regression"

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "Sales-Transaction-v4a_preprocessing.csv"}
    command: "python modelling.py --data_path {data_path}"
